{
  "name": "exo-inventory-app",
  "version": "1.0.0",
  "private": true,
  "description": "API to pull product data from MYOB Exo, store in DB, and view via frontend",
  "main": "index.js",
  "scripts": {
    "postinstall": "prisma generate",
    "prestart": "prisma migrate deploy",
    "start": "node index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "prisma": "^6.12.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
